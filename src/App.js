import React from "react";
import Layout from "./core/Layout";

const Home = () => {
  return (
    <div>
      <Layout>
        <h1>This is the boilerplate</h1>
      </Layout>
    </div>
  );
};

export default Home;
